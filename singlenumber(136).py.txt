class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        # dict1={}
        # for num in nums:
        #     if num not in dict1:
        #         dict1[num]=1
        #     else:
        #         dict1[num]+=1
        # for key,value in dict1.items():
        #     if value==1:
        #         return key
        result=0
        for num in nums:
            result^=num(XOR)
        return result

        